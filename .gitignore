/target
.env*
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
/fuzz/coverage

# Large real-world JSON fixtures (hundreds of MiB) — never commit to this repo.
# See benches/README.md for optional local / external-bench-repo workflow.
/benches/**/*.json
/benches/datasets/
/benches/data/

# Allocator / heap profiling artifacts (examples/alloc_profile, heaptrack)
/dhat-heap.json
/heaptrack.*.gz
/heaptrack.*.raw
