# jshift-derive

Procedural derive macros for [`jshift`](https://crates.io/crates/jshift).

You normally depend on `jshift` itself; this crate is re-exported as
`jshift::JsonMutatorSchema`.

```toml
[dependencies]
jshift = "0.1"
```

## License

MIT OR Apache-2.0
